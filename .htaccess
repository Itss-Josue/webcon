<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # 🔹 Ajusta la ruta base según la carpeta de tu proyecto en el hosting
    # Si tu sistema está en public_html/webcon/ usa esta:
    RewriteBase /webcon/

    # Redirigir la raíz "/" al login de admin
    RewriteRule ^$ index.php?route=admin:login [L,QSA]

    # Todas las demás rutas van a index.php manteniendo "route"
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
